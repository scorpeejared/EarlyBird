PySide6>=6.7
PySide6-Fluent-Widgets>=1.7
playwright>=1.45
plyer>=2.1
keyring>=25.0
# Screenshot import talks to whichever AI the user configures; both SDKs are
# imported lazily, so a missing one only disables that provider.
google-genai>=2.16
openai>=2.0
pywinauto>=0.6.8; sys_platform == "win32"
pywin32>=306; sys_platform == "win32"
