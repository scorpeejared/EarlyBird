"""EarlyBird application package.

Modules inside this package import each other with relative imports;
``main.py`` at the project root is the only place that imports them by
their absolute ``src.*`` names.
"""
